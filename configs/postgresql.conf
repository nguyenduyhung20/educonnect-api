# Adjust shared_buffers based on available memory (e.g., 25% of total RAM)
shared_buffers = 2GB

# Increase work_mem for complex queries
work_mem = 16MB

# Adjust effective_cache_size based on available memory (e.g., 50% of total RAM)
effective_cache_size = 4GB

# Increase max_connections based on your workload requirements
max_connections = 200

# Enable query logging for performance analysis
# log_statement = 'all'
# log_min_duration_statement = 0
